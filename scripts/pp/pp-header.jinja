/**
 * {{ declr["hdr_fname"] }} - Autogenerated pretty-print helper functions
 *
 * Copyright (C) Simon A. F. Lund <simon.lund@samsung.com>
 * SPDX-License-Identifier: Apache-2.0
 *
 * @headerfile {{ declr["hdr_fname"] }}
 */
#ifndef __{{ declr["hdr_name"] }}_H
#define __{{ declr["hdr_name"] }}_H
#include <{{ declr["fname"] }}>
{% for ent in declr["ents"] if ent["kind"] == "g"  %}
/**
 * Produces a string representation of the given ::{{ ent["name"] }}
 *
 * @param eval the enum value to produce a string representation of
 * @return On success, a string representation is returned. On error, the string
 * "ENOSYS" is returned.
 */
const char *
{{ ent["name"] }}_str(enum {{ ent["name"] }} eval);
{% endfor %}
#endif /* __{{ declr["hdr_name"] }}_H */
